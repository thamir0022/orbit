export enum ProjectStatus {
  IDEA = 'idea',
  DISCOVERY = 'discovery',
  PROPOSAL = 'proposal',
  ACCEPTED = 'accepted',
  READY = 'ready',
  ACTIVE = 'active',
  PAUSED = 'paused',
  MAINTENANCE = 'maintenance',
  COMPLETED = 'completed',
  CANCELED = 'canceled',
}
